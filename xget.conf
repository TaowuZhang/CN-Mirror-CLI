# 默认前缀：将其改为你在 Cloudflare Workers 部署的 Xget 域名
# 示例：XGET_PREFIX="https://xget.example.workers.dev/"
# 注意：CN-Mirror-CLI 使用 GHProxy 风格前缀（前缀后直接拼接原始 URL）
# 如果你的 Worker 仅支持平台前缀路径（如 /xget/gh/...），请勿在此使用；改用 xget-local 或让我为脚本添加路径模式支持。
XGET_PREFIX="https://540383401.xyz/xget/"

# 默认 Homebrew 镜像站（tuna|ustc|bfsu）
XGET_BREW_MIRROR_DEFAULT="bfsu"